{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "just sync",
            "type": "shell",
            "command": "just sync",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "just test",
            "type": "shell",
            "command": "just test",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}
